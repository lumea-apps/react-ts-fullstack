{
  "name": "server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server-node.ts",
    "dev:wrangler": "npx wrangler dev",
    "build": "npx wrangler deploy --dry-run --outdir dist",
    "deploy": "npx wrangler deploy",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "db:auth:generate": "npx @better-auth/cli generate --config src/lib/auth.config.ts --output src/db/auth-schema.ts --yes",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun scripts/seed.ts",
    "db:setup": "bun scripts/setup.ts"
  },
  "dependencies": {
    "@better-auth/expo": "^1.4.15",
    "@hono/node-server": "^1.13.8",
    "@hono/zod-validator": "^0.5.0",
    "better-auth": "^1.4.14",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.7.0",
    "postgres": "^3.4.8",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "@types/node": "^22.14.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
