# =============================================================================
# Vite Client Environment Variables
# =============================================================================
# Copy to .env.local and customize.
# Only variables prefixed with VITE_ are exposed to the client.
# =============================================================================

# API URL (optional - auto-detected in Daytona sandboxes)
# Uncomment and set if you need to override the default
# VITE_API_URL=http://localhost:3001
